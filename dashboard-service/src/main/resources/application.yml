spring:
  application:
    name: dashboard-service

server:
  port: ${SERVER_PORT:8087}

management:
  endpoints:
    web:
      exposure:
        include: health,info

dashboard:
  sensors:
    update-interval-ms: 5000
  shuttle:
    update-interval-ms: 7000